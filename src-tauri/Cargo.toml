[package]
name = "keystroke-counter"
version = "0.1.0"
description = "Cross-platform keystroke counter app"
authors = ["kissung"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
name = "keystroke_counter_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
# Tauri core with tray icon support
tauri = { version = "2", features = ["tray-icon", "image-png"] }
tauri-plugin-opener = "2"
tauri-plugin-positioner = { version = "2", features = ["tray-icon"] }

# Serialization
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Global keyboard hook (cross-platform)
# Using fork that fixes macOS TIS/TSM API threading crash
# See: https://github.com/tauri-apps/tauri/discussions/7839
rdev = { git = "https://github.com/fufesou/rdev" }

# SQLite database
rusqlite = { version = "0.31", features = ["bundled"] }

# Date/time handling
chrono = { version = "0.4", features = ["serde"] }

# Async runtime
tokio = { version = "1", features = ["sync", "rt-multi-thread"] }

# Logging
log = "0.4"
env_logger = "0.11"

[target.'cfg(target_os = "macos")'.dependencies]
cocoa = "0.26"
objc = "0.2"
core-foundation = "0.10"

